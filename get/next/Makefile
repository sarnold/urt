#
# Generated by the NeXT Interface Builder.
#
# NOTE: Do NOT change this file -- Interface Builder maintains it.
#
# Put all of your customizations in files called Makefile.preamble
# and Makefile.postamble (both optional), and Makefile will include them.
#

NAME = RLEViewer

INTERFACES = RLEViewer.nib
CLASSES = Image.m Myapp.m RASF.m RLE.m
MFILES = RLEViewer_main.m
TIFFFILES = app.icon.tiff dart.tiff eps.tiff epsH.tiff rasf.icon.tiff \
            rle.icon.tiff tiff.icon.tiff tiff.iconH.tiff
OTHERSRCS = ChangeLog
APPICON = app.icon.tiff
DOCICONS = rle.icon.tiff rasf.icon.tiff

SOURCEMODE = 444

LIBS = -lrle\
	-lNeXT_s -lsys_s
DEBUG_LIBS = $(LIBS)
PROF_LIBS = -lrle\
	-lNeXT_p -lsys_p

MAKEFILEDIR = /usr/lib/nib
ICONSECTIONS =	-sectcreate __ICON app app.icon.tiff \
		-sectcreate __ICON rle rle.icon.tiff \
		-sectcreate __ICON rasf rasf.icon.tiff

INSTALLDIR = $(HOME)/Apps
INSTALLFLAGS = -c -s -m 755

-include Makefile.preamble

include $(MAKEFILEDIR)/app.make

-include Makefile.postamble

-include Makefile.dependencies
